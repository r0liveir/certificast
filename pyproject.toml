[project]
name = "certificast"
version = "1.0.0"
description = ""
readme = "README.md"
authors = [
    { name = "wate", email = "ryan.o2712@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-pptx>=1.0.2",
]

[project.scripts]
certificast = "certificast.cli:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
